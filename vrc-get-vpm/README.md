# `vrc-get-vpm`

The VPM client library used by ALCOMD3.

Since VPM itself is not stable, this library is not stable either.

This library is maintained as part of the ALCOMD3 repository. The directory
name is kept for compatibility with the existing Rust workspace and package
layout.
